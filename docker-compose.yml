version: "3.8"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: endosys
      POSTGRES_USER: endosys
      POSTGRES_PASSWORD: endosys
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: